import { wait } from 'gml-utils';
const events = [];

// events.push({
//     go: 'spinStartButton',
//     event: 'spin-start-tap',
//     exe: function (sys, gos) {
//         if (this.getState() === 'idle') {
//             this.enter('spinStart');
//         }
//     }
// });

export default events;

